<% if paginate_params(@response).num_pages > 1 %>
 <div class="row record-padding">
  <div class="span16"> 
    <div class="pagination pagination-top hidden-phone">
      <%= paginate_rsolr_response @response, :outer_window => 2, :theme => 'blacklight' %>
    </div>
  </div>
 </div>
<% end %>
